<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalhouse.workshop">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hamburguer"
        android:label="Digital Burguer"
        android:roundIcon="@drawable/hamburguer"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name="view.TelaInicialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="view.HomeActivity" />
        <activity android:name="view.MainActivity"></activity>
    </application>

</manifest>